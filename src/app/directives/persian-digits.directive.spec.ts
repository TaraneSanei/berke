import { PersianDigitsDirective } from './persian-digits.directive';

describe('PersianDigitsDirective', () => {
  it('should create an instance', () => {
    // const directive = new PersianDigitsDirective();
    // expect(directive).toBeTruthy();
  });
});
